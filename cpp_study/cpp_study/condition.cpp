#include <iostream>

// ? 연산자
//int main() {
//	int a = 10, b = 20;
//	int score = 92;
//	int result = 0;
//
//	result = (a > b) ? a : b;
//	std::cout << result << std::endl; // 20이 저장됨 
//
//	result = (a > b) ? (a++) : (b--);
//	std::cout << "a = " << a << ", b = " << b << std::endl;
//
//	result = (a < b) ? (b - a) : (a - b);
//	std::cout << result << std::endl; // b - a인 9이 저장됨
//
//	result = (a != 0) ? (a / b) : 0;
//	std::cout << ((score > 90) ? "Excellent!" : "Good!");
//}